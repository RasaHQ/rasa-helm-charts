{{- if and .Values.rasaProServices.enabled .Values.rasaProServices.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rasa.fullname" . }}-rasa-pro-services
  labels:
    {{- include "rasa.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rasa.fullname" . }}-rasa-pro-services
  minReplicas: {{ .Values.rasaProServices.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.rasaProServices.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.rasaProServices.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.rasaProServices.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.rasaProServices.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
          target:
          type: Utilization
          averageUtilization: {{ .Values.rasaProServices.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
