name: Release Studio Helm Chart

on:
  push:
    branches:
      - main
    paths:
      - 'charts/studio/**'
      - '!charts/rasa/**'
      - '!replicated/**'

env:
  ARTIFACT_REGISTRY: europe-west3-docker.pkg.dev
  HELM_REPOSITORY: rasa-releases
  HELM_CHART: helm-charts

jobs:
  release:
    runs-on: ubuntu-24.04
    outputs:
      studio-version: ${{ steps.chart-info.outputs.version }}
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0

      - name: Install Helm 3
        uses: azure/setup-helm@5119fcb9089d432beecbf79bb2c7915207344b78
        with:
          version: v3.15.2

      - name: Authenticate to Google Cloud
        id: google-auth
        uses: google-github-actions/auth@71fee32a0bb7e97b4d33d548e7d957010649d8fa
        with:
          token_format: access_token
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER_OIDC_HELM }}
          service_account: '${{ secrets.SERVICE_ACCOUNT_OIDC_HELM }}'

      - name: Read Helm Chart
        id: chart-info
        uses: miraai/read-helm-chart-yaml@9a798908719da8d062b86097d4ff1ad85abfc7aa
        with:
          path: charts/studio/

      - name: Package and push chart
        id: release
        env:
          ACCESS_TOKEN: ${{ steps.google-auth.outputs.access_token }}
          CHART_PATH: charts/studio
          CHART_URI: oci://${{ env.ARTIFACT_REGISTRY }}/${{ env.HELM_REPOSITORY }}/${{ env.HELM_CHART }}
          CHART_TAR: ${{ steps.chart-info.outputs.name }}-${{ steps.chart-info.outputs.version }}.tgz
          REGISTRY_URL: https://${{ env.ARTIFACT_REGISTRY }}
        run: |
          echo "${ACCESS_TOKEN}" | helm registry login -u oauth2accesstoken --password-stdin "${REGISTRY_URL}"
          helm dependency update "${CHART_PATH}"
          helm package "${CHART_PATH}"
          helm push "${CHART_TAR}" "${CHART_URI}"
          echo "uri=${CHART_URI}" >> $GITHUB_OUTPUT

      - name: Notify Slack of successful release 💬
        if: steps.release.outcome == 'success'
        uses: slackapi/slack-github-action@007b2c3c751a190b6f0f040e47ed024deaa72844
        with:
          # Send notication to #release slack channel
          channel-id: 'C024Z61K9QU'
          slack-message: "🚀 New *Studio* Helm Chart version `${{ steps.chart-info.outputs.name }}-${{ steps.chart-info.outputs.version }}` has been released!"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

      - name: Notify Slack of unsuccessful release ⛔️
        if: steps.release.outcome == 'failure'
        uses: slackapi/slack-github-action@007b2c3c751a190b6f0f040e47ed024deaa72844
        with:
          # Send notication to #release slack channel
          channel-id: 'C024Z61K9QU'
          slack-message: "⛔️ *Studio* Helm Chart version `${{ steps.chart-info.outputs.name }}-${{ steps.chart-info.outputs.version }}` could not be released 😱! Please check out <https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|Github Action workflow log>"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          
      - name: Get previous release tag
        id: previous-release
        uses: JinoArch/get-latest-tag@4a55da58d123a16b749c152b45c15f1ead139d42

      - name: Create Studio Release Tag
        uses: rickstaa/action-create-tag@55450aa830ede05bcbb37fbdad678e36c73e9d15
        with:
          tag: "${{ steps.chart-info.outputs.name }}-${{ steps.chart-info.outputs.version }}"
          tag_exists_error: false

      - name: Build Studio changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@6fd5cc6eaf7567dbd0f9666061215bb476f012fc
        with:
          configurationJson: |
            {
              "categories": [
                {
                    "title": "## 🚀 Studio Helm Chart",
                    "labels": ["studio"]
                }
              ]
            }
          fromTag: "${{ steps.previous-release.outputs.latestTag }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Studio Release
        uses: mikepenz/action-gh-release@ac23f2c4388204c6081863ead928a6f05fd7838a
        with:
          tag_name: ${{ steps.chart-info.outputs.name }}-${{ steps.chart-info.outputs.version }}
          body: ${{ steps.changelog.outputs.changelog }}
      
  replicated-release:
    name: Promote Replicated release
    needs: [release]
    uses: RasaHQ/infrasec-workflows/.github/workflows/promote-replicated-release.yml@main
    with:
      replicated-channel: Stable
      replicated-version: ${{ needs.release.outputs.studio-version }}
      studio-version: ${{ needs.release.outputs.studio-version }}
    secrets: inherit
