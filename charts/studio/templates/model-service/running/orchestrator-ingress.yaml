{{- if and .Values.modelService.running.enabled .Values.modelService.running.orchestrator.ingress.enabled -}}
{{- $fullName := include "studio.fullname" . -}}
{{- $svcPort := .Values.modelService.running.orchestrator.service.port -}}
{{- if and .Values.modelService.running.orchestrator.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.modelService.running.orchestrator.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.modelService.running.orchestrator.ingress.annotations "kubernetes.io/ingress.class" .Values.modelService.running.orchestrator.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: model-running-service-orchestrator
  labels:
    {{- include "studio.labels" . | nindent 4 }}
    {{- if .Values.modelService.running.orchestrator.ingress.labels }}
{{ toYaml .Values.modelService.running.orchestrator.ingress.labels | indent 4 }}
    {{- end }}
  {{- with .Values.modelService.running.orchestrator.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.modelService.running.orchestrator.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.modelService.running.orchestrator.ingress.className }}
  {{- end }}
  {{- if .Values.modelService.running.orchestrator.ingress.tls }}
  tls:
    {{- range .Values.modelService.running.orchestrator.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.modelService.running.orchestrator.ingress.hosts }}
    - host: {{ $.Values.modelService.running.orchestrator.ingress.hostName  | default $.Values.config.ingressHost | quote }}
      http:
        paths:
          - path: {{ $.Values.modelService.running.proxy.botTalkSubPath }}
            pathType: Prefix
            backend:
              service:
                name: nginx-reverse-proxy
                port:
                  number: {{ $.Values.modelService.running.proxy.service.port }}
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: model-running-service
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: model-running-service
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
          {{- if .extraPaths }}
          {{- range .extraPaths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ .serviceName }}
                port:
                  number: {{ .servicePort }}
              {{- else }}
              serviceName: {{ .serviceName }}
              servicePort: {{ .servicePort }}
              {{- end }}
          {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
