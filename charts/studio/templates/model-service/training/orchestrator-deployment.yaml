{{- if .Values.modelService.training.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-training-service-orchestrator
  labels:
    {{- include "studio.labels" . | nindent 4 -}}
    app.kubernetes.io/component: model-training-service
    {{- if .Values.deploymentLabels }}
      {{- toYaml .Values.deploymentLabels | nindent 4 }}
    {{- end }}
  {{- with .Values.deploymentAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.modelService.training.orchestrator.replicaCount }}
  selector:
    matchLabels:
      {{- include "modelTrainingService.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.modelService.training.orchestrator.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "modelTrainingService.selectorLabels" . | nindent 8 }}
        {{- if .Values.podLabels }}
          {{ toYaml .Values.podLabels | nindent 8 }}
        {{- end }}
    spec:
      hostNetwork: {{ .Values.hostNetwork }}
      dnsPolicy: {{ include "studio.dnsPolicy" . }}
      {{- if .Values.dnsConfig }}
      dnsConfig:
        {{ toYaml .Values.dnsConfig | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "modelTrainingService.serviceAccountName" . }}
      {{- if .Values.modelService.training.orchestrator.podSecurityContext.enabled }}
      securityContext:
        {{- omit .Values.modelService.training.orchestrator.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      containers:
        - name: model-training-service-orchestrator
          {{- if .Values.modelService.training.orchestrator.securityContext.enabled }}
          securityContext:
            {{- omit .Values.modelService.training.orchestrator.securityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          image: {{ include "modelTrainingService.orchestrator.image" . }}
          imagePullPolicy: {{ .Values.modelService.training.orchestrator.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.modelService.training.orchestrator.service.port }}
              protocol: TCP
          {{- if or .Values.modelService.training.orchestrator.livenessProbe.enabled }}
          livenessProbe:
            {{- omit .Values.modelService.training.orchestrator.livenessProbe "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if or .Values.modelService.training.orchestrator.readinessProbe.enabled }}
          readinessProbe:
            {{- omit .Values.modelService.training.orchestrator.readinessProbe "enabled" | toYaml | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.modelService.training.orchestrator.resources | nindent 12 }}
          volumeMounts:
          {{- include "gcpCredentials.volumeMount" . | nindent 12 -}}
          {{- if .Values.modelService.training.orchestrator.volumeMounts }}
            {{- toYaml .Values.modelService.training.orchestrator.volumeMounts | nindent 12 }}
          {{- end }}
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/config/gcloud/credentials.json"
            {{- range $key, $value := .Values.modelService.training.orchestrator.environmentVariables }}
            - name: {{ $key | upper }}
              {{- if $value.value }}
              value: {{ $value.value | quote }}
              {{- else if $value.secret }}
              valueFrom:
                secretKeyRef:
                  name: {{ $value.secret.name }}
                  key: {{ $value.secret.key }}
              {{- end }}
            {{- end }}
          {{- if .Values.modelService.training.orchestrator.envFrom }}
          envFrom:
            {{- toYaml .Values.modelService.training.orchestrator.envFrom | nindent 12 }}
          {{- end }}
      {{- if .Values.modelService.training.orchestrator.additionalContainers }}
        {{- toYaml .Values.modelService.training.orchestrator.additionalContainers | nindent 8 }}
      {{- end }}
      volumes:
      {{- include "gcpCredentials.volume" . | nindent 8 -}}
      {{- if .Values.modelService.training.orchestrator.volumes }}
        {{- toYaml .Values.modelService.training.orchestrator.volumes | nindent 8 }}
      {{- end }}
      {{- with .Values.modelService.training.orchestrator.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.modelService.training.orchestrator.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.modelService.training.orchestrator.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}    
{{- end }}
