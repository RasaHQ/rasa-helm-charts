{{- if .Values.app.ingress.enabled -}}
{{- $fullName := include "studio.fullname" . -}}
{{- $svcPort := .Values.app.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-app
  labels:
    {{- include "studio.labels" . | nindent 4 }}
    {{- if .Values.app.ingress.labels }}
{{ toYaml .Values.app.ingress.labels | indent 4 }}
    {{- end }}
  {{- include "app.ingress.annotations" . | nindent 2 }}
spec:
  {{- $className := .Values.app.ingress.className | default .Values.config.ingressClassName }}
  {{- if $className }}
  ingressClassName: {{ $className }}
  {{- end }}
  {{- if .Values.app.ingress.tls }}
  tls:
    {{- range .Values.app.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.app.ingress.hostName | default $.Values.config.ingressHost | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}-app
                port:
                  number: {{ $svcPort }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}-app
                port:
                  number: {{ $svcPort }}
{{- end }}
