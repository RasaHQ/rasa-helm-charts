{{- if .Values.actionServer.ingress.enabled -}}
{{- $fullName := include "rasa.fullname" . -}}
{{- $svcPort := .Values.actionServer.service.port -}}
{{- if and .Values.actionServer.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.actionServer.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.actionServer.ingress.annotations "kubernetes.io/ingress.class" .Values.actionServer.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-action-server
  labels:
    {{- include "rasa.labels" . | nindent 4 }}
    {{- if .Values.actionServer.ingress.labels }}
{{ toYaml .Values.actionServer.ingress.labels | indent 4 }}
    {{- end }}
  {{- with .Values.actionServer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.actionServer.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.actionServer.ingress.className }}
  {{- end }}
  {{- if .Values.actionServer.ingress.tls }}
  tls:
    {{- range .Values.actionServer.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.actionServer.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-action-server
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-action-server
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
          {{- if .extraPaths }}
          {{- range .extraPaths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ .serviceName }}
                port:
                  number: {{ .servicePort }}
              {{- else }}
              serviceName: {{ .serviceName }}
              servicePort: {{ .servicePort }}
              {{- end }}
          {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
