{{- if and .Values.rasa.enabled .Values.rasa.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rasa.fullname" . }}
  labels:
    {{- include "rasa.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rasa.fullname" . }}
  minReplicas: {{ .Values.rasa.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.rasa.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.rasa.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.rasa.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.rasa.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.rasa.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
