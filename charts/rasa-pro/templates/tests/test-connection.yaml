apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rasa-pro.fullname" . }}-test-connection"
  labels:
    {{- include "rasa-pro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rasa-pro.fullname" . }}:{{ .Values.rasaPro.service.port }}']
  restartPolicy: Never
