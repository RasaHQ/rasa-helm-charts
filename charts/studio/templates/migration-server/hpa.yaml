{{- if .Values.migration-server.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "studio.fullname" . }}
  labels:
    {{- include "studio.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "studio.fullname" . }}
  minReplicas: {{ .Values.migration-server.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.migration-server.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.migration-server.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.migration-server.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.migration-server.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.migration-server.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
