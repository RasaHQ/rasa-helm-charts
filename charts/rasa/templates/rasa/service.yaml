{{- if .Values.rasa.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rasa.fullname" . }}
  labels:
    {{- include "rasa.labels" . | nindent 4 }}
  {{- with .Values.rasa.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.rasa.service.type }}
  {{- if or (eq .Values.rasa.service.type "LoadBalancer") (eq .Values.rasa.service.type "NodePort") }}
  externalTrafficPolicy: {{ .Values.rasa.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq .Values.rasa.service.type "LoadBalancer") (not (empty .Values.rasa.service.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.rasa.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.rasa.service.port }}
      targetPort: {{ .Values.rasa.service.targetPort }}
      protocol: TCP
      name: http
      {{- if and (or (eq .Values.rasa.service.type "NodePort") (eq .Values.rasa.service.type "LoadBalancer")) (not (empty .Values.rasa.service.nodePort)) }}
      nodePort: {{ .Values.rasa.service.nodePort }}
      {{- end }}
  selector:
    {{- include "rasa.selectorLabels" . | nindent 4 }}
{{- end }}
