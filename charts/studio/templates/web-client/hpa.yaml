{{- if .Values.web-client.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "studio.fullname" . }}
  labels:
    {{- include "studio.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "studio.fullname" . }}
  minReplicas: {{ .Values.web-client.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.web-client.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.web-client.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.web-client.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.web-client.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.web-client.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
